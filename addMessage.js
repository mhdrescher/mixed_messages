// Setting up factory functions to add items to the respective message container 

function addMessage1(message) {
    randomMessages1.push({
        message: message
    });
}

function addMessage2(message) {
    randomMessages2.push({
        message: message
    });
}

function addMessage3(message) {
    randomMessages3.push({
        message: message
    });
}
